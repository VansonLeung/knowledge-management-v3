# Docling Document Conversion Service
# Install with: pip install -r requirements.txt

# Core Docling
docling>=2.0.0

# Optional: MLX support for macOS Apple Silicon (uncomment if needed)
docling[mlx]>=2.0.0

# Optional: Audio transcription with Whisper (uncomment if needed)
docling[asr]>=2.0.0

# FastAPI server
fastapi>=0.110.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Data handling
pandas>=2.0.0
tabulate>=0.9.0  # For DataFrame.to_markdown()
